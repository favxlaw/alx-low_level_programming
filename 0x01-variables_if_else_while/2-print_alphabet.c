#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: Got a random number and check its las digit, compare it with a
 * Return: o
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')

	{
		putchar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
